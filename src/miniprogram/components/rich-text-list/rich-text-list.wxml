<view >
    <block wx:for="{{list}}" wx:key="key">
        <rich-text-item item="{{item}}" index="{{index}}" bind:addItem="handleAddItem" bind:changeVale="changeItemValue"></rich-text-item>
    </block>
</view>
